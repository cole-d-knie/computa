---
description: "Use when the user asks for swarm verification, phased TDD execution, adversarial review, judge/verifier review, runtime QA, or audit-ready implementation."
globs:
alwaysApply: false
---

# Swarm Verify

When this rule applies, execute the task with a phased, evidence-first verification loop.

Core rules:

- Save the raw user request immediately to an external `user-task.md` before rewriting or planning from memory.
- Create all plan, phase, task, subtask, issue, blocker, evidence, and final report artifacts outside the repo unless the user explicitly asks otherwise.
- Phase 0: create the plan directory, `plan.md`, `master-task-ledger.csv`, `issues-and-blockers.md/csv`, phase directories, task directories, subtask ledgers, and `reports/`.
- Phase 1: investigate before implementation. Read source truth, verify current and expected behavior, identify edge cases and coverage gaps, run baseline commands, reproduce the issue or prove the gap, and create failing evidence where possible.
- Use TDD for behavior changes: failing test first, smallest fix, passing test after.
- Run unit, integration, smoke, runtime, and browser-visible QA as relevant. Use the available browser automation tools for real UI checks.
- Run swarms only on independent work. Never parallelize overlapping files, shared servers, browser sessions, databases, dashboards, queues, deployments, migrations, or dependent subtasks.
- After every task: run one adversarial reviewer per subtask, then one judge/verifier per subtask. Only implement approved recommendations.
- After every phase: run one adversarial reviewer per task, then one judge/verifier per task. Do not close the phase until each task is approved complete, approved deferred, or blocked with evidence.
- Keep CSV and Markdown ledgers current. Mark work done only after evidence exists.
- Make one commit per completed task only after verification passes and commits are allowed.
- Do not push unless explicitly asked.

Final reports required under `reports/`:

- `final-summary.md`
- `verification-needed.md`
- `blockers-and-open-issues.md`
- `task-gap-analysis.md`
- `new-issues-found.md`

Reports may be combined only when clearer, but every category must remain present as an explicit heading.

Do not claim completion unless the final response includes exact verification commands, failing-before evidence or documented exception, passing-after evidence, runtime QA, edge cases, report paths, remaining risks, gaps, blockers, new issues fixed, and new issues not fixed.

Golden rule: evidence before assertions. Never say "it works" without verification command output and runtime proof.
